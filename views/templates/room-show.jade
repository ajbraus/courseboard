.event-name
  form(ng-submit="enterRoom()")
    a.hash-tag.pull-left(href="/") #
    input.hash-input(ng-model="room.name", placeholder="EventName", ng-space, autofocus)
    button.pull-right.btn.qq-btn.enter-room-btn#enterRoom(ng-click="enterRoom()")
      i.icon.ion-log-in
div.how-it-works
  | Hi, 
  b Welcome to Question Queue. 
  br
  | Enter your #EventName above to ask questions and vote on them.